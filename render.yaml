services:
  - type: web
    name: multi-agent-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run web_app.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src